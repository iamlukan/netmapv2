# Netmap Environment Variables

# SSH Tunnel Configuration
# Host that acts as the SSH Gateway (Jumpbox)
SSH_HOST=192.168.1.100

# User for SSH connection
SSH_USER=admband

# Path to SSH Key (Local path on your host machine)
# Example Windows: C:/Users/YourUser/.ssh/id_ed25519
SSH_KEY_PATH=C:/Users/ExampleUser/.ssh/id_ed25519

# OCS Database Configuration
# Option A: Full URL (Legacy)
# OCS_DATABASE_URL=mysql+pymysql://ocs:pass@netmap-tunnel:5432/ocsweb

# Option B: Component Parts (Recommended for portability)
# If using Tunnel: OCS_DB_HOST=netmap-tunnel
# If using Prod:   OCS_DB_HOST=localhost (or real IP)
OCS_DB_HOST=netmap-tunnel
OCS_DB_USER=ocs
OCS_DB_PASS=ocs
OCS_DB_NAME=ocsweb
OCS_DB_PORT=3306
