# Netmap Environment Variables

# SSH Tunnel Configuration
# Host that acts as the SSH Gateway (Jumpbox)
SSH_HOST=192.168.1.100

# User for SSH connection
SSH_USER=admband

# Path to SSH Key (Local path on your host machine)
# Example Windows: C:/Users/YourUser/.ssh/id_ed25519
# Example Linux/Mac: /home/user/.ssh/id_ed25519
# Example Linux/Mac: /home/user/.ssh/id_ed25519
SSH_KEY_PATH=C:/Users/ExampleUser/.ssh/id_ed25519

# OCS Database Configuration (Optional)
# Point to the mapped tunnel port (e.g., localhost:5433)
# If running on the same network or checking tunneled connection:
OCS_DATABASE_URL=mysql+pymysql://ocs_user:ocs_password@localhost:5433/ocsweb
# Note: For accessing OCS inside Docker via tunnel, you might need to use host.docker.internal depending on setup, 
# but usually we map the tunnel to localhost:5433 so checking from host works. 
# If checking FROM netmap-app container, it needs to hit the netmap-tunnel service name if on same network, 
# OR if using the SSH tunnel mapped to host, use host.docker.internal or the container name if in same compose network.
# IN THIS SETUP: netmap-app and netmap-tunnel are in same network. 
# Usage from app container: OCS_DATABASE_URL=mysql+pymysql://ocs:pass@netmap-tunnel:5432/ocsweb ? 
# Wait, the tunnel maps remote 5432 to local 5433 (Host) and standard export 5432 (Container).
# Inside the docker network, netmap-tunnel exposes 5432.
# So correct container-to-container URL: mysql+pymysql://ocs:pass@netmap-tunnel:5432/ocsweb
